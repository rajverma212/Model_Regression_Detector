# Runtime deps for the Vercel Python serverless function (the MRDS HTTP API).
# uvicorn/streamlit are intentionally excluded: Vercel provides the ASGI server, and
# the API never imports the dashboard.
fastapi>=0.110
pydantic>=2.6
pydantic-settings[yaml]>=2.2
pyyaml>=6.0
anthropic>=0.69
jinja2>=3.1
# libSQL/Turso storage backend — used when MRDS_STORAGE_BACKEND=libsql is set on the
# deployment (durable, cross-instance persistence). Harmless otherwise.
libsql>=0.1
